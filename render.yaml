services:
  - type: web
    name: debtlens-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    plan: free
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
